# gateway/nginx.conf

worker_processes 1;

events { worker_connections 1024; }

http {
    sendfile on;
    server {
        listen 80;

        client_max_body_size 1000M;

        location /fastapi {
            proxy_pass http://backend:8000;
        }
    }
}
